# 智能晚辅托管系统开发和测试计划

文档版本：MVP V1.0  
生成日期：2026-05-01  
适用范围：晚辅托管系统 MVP 版本开发、测试、交付和上线准备  
需求基准：《智能晚辅托管系统MVP版本需求分析说明书》

---

## 1. 技术栈详细说明

### 1.1 总体架构

MVP 采用 Web 管理端 + 老师 Web/平板工作台 + 家长 H5 + 学生 H5/平板端 + 后端 API + AI 服务编排的架构。

推荐采用前后端分离、单仓库 Monorepo 管理：

- 前端：统一 React/Next.js 技术栈，按角色拆分路由和权限。
- 后端：Node.js/NestJS 提供业务 API、权限、文件、AI 编排和异步任务。
- 数据库：PostgreSQL 保存核心业务数据。
- 缓存和任务队列：Redis 处理验证码、短期缓存、异步通知和 AI 任务状态。
- 文件存储：S3 兼容对象存储，用于到托照片、作业原图、批改图、练习单 Word 文件。
- AI 能力：通过 AI Provider Adapter 接入大模型、图像理解、OCR/圈错建议和同类题生成。
- 部署：MVP 生产环境锁定为单台 Ubuntu Server + Docker Compose + Nginx 反向代理 + HTTPS + 定时备份，后续再按规模升级为多服务器或 Kubernetes。

Ubuntu 生产部署基线：

- 操作系统：Ubuntu Server 22.04 LTS 或 24.04 LTS。
- 编排方式：Docker Compose 统一编排 `web`、`api`、`postgres`、`redis`、`minio`、`nginx`。
- 反向代理：Nginx 对外暴露 `80/443`，API 和 Web 走域名访问。
- HTTPS：使用 Let's Encrypt / Certbot，证书续期纳入上线检查。
- 生产目录：统一使用 `/opt/afterclass` 存放应用、配置、Compose 文件、日志目录和运维脚本。
- 端口策略：仅开放 `80/443`，PostgreSQL、Redis、MinIO 管理端默认不对公网开放。
- 启动策略：Docker 随系统启动，Compose 服务使用 `restart: unless-stopped`。
- 运维策略：配置日志轮转、防火墙、数据库备份、文件备份、恢复演练和磁盘监控。

建议目录：

```text
apps/
  web/                # 管理端、老师端、家长端、学生端统一前端
  api/                # 后端 API
packages/
  db/                 # Prisma schema、迁移、seed
  shared/             # 类型、枚举、权限常量、校验规则
  ui/                 # 设计系统组件
  ai/                 # AI provider adapter、prompt、结构化输出 schema
```

### 1.2 前端技术栈

核心选择：

- Next.js App Router：用于多角色 Web/H5 路由、服务端渲染、权限保护和页面分包。
- React + TypeScript：统一组件模型和类型约束。
- Tailwind CSS：实现 Serenity-Neumorphic 设计风格、响应式布局和设计 token。
- shadcn/ui 或 Radix UI：用于 Dialog、Select、Tabs、Popover、Toast、Form 等基础可访问组件。
- TanStack Query：处理客户端数据请求、缓存、乐观更新和刷新。
- React Hook Form + Zod：处理表单、校验和错误提示。
- Lucide Icons：统一图标风格，不使用 emoji 作为功能图标。

端适配：

- 管理端：桌面优先，左侧导航、顶部筛选、数据卡片、表格、图表。
- 老师端：平板优先，支持高频操作、批量确认、拍照上传、触控友好。
- 家长端：手机 H5 优先，底部导航、卡片式信息、图片查看、AI 助手输入。
- 学生端：手机/平板优先，大按钮、大字号、任务进度、错题入口。

设计实现要求：

- 使用统一 design tokens：颜色、圆角、阴影、间距、字体层级。
- Serenity-Neumorphic 风格作为第一版 UI 基准：柔和浅色背景、内外阴影、圆角卡片、低饱和辅助色。
- 移动端点击目标不小于 44px。
- 表格、卡片、图片容器必须有稳定尺寸，避免内容加载造成布局跳动。
- 家长端作业详情必须同时支持作业原图、批改图片和三类点评展示。

### 1.3 后端技术栈

核心选择：

- Node.js + NestJS：模块化组织业务域，便于权限、队列、AI 服务拆分。
- TypeScript：前后端共享类型，减少接口不一致。
- Prisma ORM：管理 PostgreSQL schema、迁移和类型生成。
- PostgreSQL：保存校区、学生、班级、考勤、作业、收费、核算、AI 日志等核心数据。
- Redis：验证码、短期会话缓存、任务队列、通知发送状态。
- BullMQ：处理异步任务，包括 AI 圈错、同类题生成、通知发送、Word 练习单生成。
- OpenAPI/Swagger：自动生成接口文档，便于前后端联调。

后端模块拆分：

```text
AuthModule              # 登录、Token、角色、会话
CampusModule            # 多校区、校区权限
UserModule              # 管理员、老师、家长、学生账号
StudentModule           # 学生档案、家长绑定、身份证号脱敏
ClassModule             # 班级、老师分配、学生分班
ServiceTypeModule       # 中午托、下午托、晚辅导、晚全托
AttendanceModule        # 学生拍照签到、请假、缺勤、签退
TeacherAttendanceModule # 老师签到、签退、补签审核
HomeworkModule          # 作业原图、批改图、AI 圈错、反馈发布
MistakeBookModule       # 错题收录、同类题、Word 练习单
FeedbackModule          # 行为表现、作业完成、知识掌握
BillingModule           # 月缴、学期缴、服务有效期、余额/欠费
SettlementModule        # 校区/班级收入、老师课费、预估毛利
NotificationModule      # 到托通知、作业反馈、服务到期提醒
AiModule                # AI 意图识别、确认卡片、操作日志
FileModule              # 图片、Word 文件、访问权限、签名 URL
AuditModule             # 敏感信息查看、AI 写入、跨校区操作日志
```

### 1.4 数据库与数据模型

数据库使用 PostgreSQL。MVP 核心表：

- `campus`：校区档案。
- `user`：系统账号。
- `role` / `user_role`：角色和用户角色关系。
- `guardian`：家长资料。
- `student`：学生档案，包含 `id_card_no_encrypted`。
- `guardian_student`：家长与学生绑定关系。
- `class`：班级，关联校区。
- `teacher_class`：老师负责班级。
- `teacher_campus`：老师授权校区。
- `service_type`：固定 4 种托管类型。
- `student_service`：学生当前托管类型和服务周期。
- `attendance_record`：学生考勤、拍照签到、通知状态。
- `teacher_attendance`：老师考勤。
- `leave_request`：请假申请。
- `homework_review`：作业批改记录，包含原图和批改图。
- `homework_review_image`：作业原图、批改图、图片类型、排序。
- `mistake_book_item`：错题本。
- `feedback`：三类今日点评。
- `billing_record`：月缴/学期缴、服务有效期、应收实收、余额/欠费。
- `class_settlement`：校区/班级核算。
- `notification`：通知记录。
- `ai_action_log`：AI 输入、意图、风险、确认和执行结果。
- `audit_log`：敏感信息查看、导出、跨校区操作。

关键数据规则：

- 所有核心业务表必须包含 `campus_id` 或可通过关联表追溯到校区。
- 学生身份证号必须加密存储，列表默认脱敏。
- 家长端不能读取余额、欠费金额、班级核算、机构收入、老师课费、班级毛利。
- 作业原图和批改图必须按权限生成短期签名 URL。
- AI 写入动作必须记录 `confirmed_by`、`confirmed_at` 和 `ai_action_log_id`。

### 1.5 文件与图片存储

对象存储建议使用 S3 兼容服务：

- 本地开发：MinIO。
- 生产环境：阿里云 OSS、腾讯云 COS、七牛云或其他 S3 兼容存储。

文件类型：

- 到托照片：`attendance/checkin/{campusId}/{date}/{studentId}.jpg`
- 作业原图：`homework/original/{campusId}/{reviewId}/{imageId}.jpg`
- 批改图片：`homework/reviewed/{campusId}/{reviewId}/{imageId}.jpg`
- Word 练习单：`mistake/worksheet/{campusId}/{worksheetId}.docx`

文件安全：

- 不把对象存储文件设为公开读。
- 前端通过后端获取带有效期的签名 URL。
- 家长端只能请求自己绑定孩子的图片。
- 老师端只能请求授权校区和负责班级的图片。
- 图片查看、身份证号完整查看、文件导出需要写入审计日志。

### 1.6 AI 技术栈

AI 能力采用 Adapter 模式，不把业务逻辑和具体模型绑定死。

AI Provider Adapter：

- `IntentRecognitionProvider`：识别家长/老师/校长输入意图。
- `HomeworkVisionProvider`：识别作业图片、提出圈错建议。
- `FeedbackDraftProvider`：生成今日点评草稿。
- `SimilarQuestionProvider`：根据错题生成同类题草稿。
- `BusinessQueryProvider`：将经营查询转为受控查询，不直接执行高风险操作。

AI 输出必须结构化：

```ts
type AiRiskLevel = "low" | "medium" | "high";

type AiActionResult = {
  intent: string;
  riskLevel: AiRiskLevel;
  entities: Record<string, unknown>;
  confidence: number;
  summary: string;
  requiresConfirmation: boolean;
  suggestedPayload?: unknown;
};
```

AI 风险规则：

- 低风险：查询考勤、作业、服务有效期、公告、授权范围内核算数据。
- 中风险：请假、留言、反馈发布、批改确认、生成练习单。
- 高风险：删除学生、修改收费金额、修改课费、批量通知、跨校区转移学生、变更学生状态。
- 高风险操作 AI 不执行，只引导到传统页面。

第一版 AI 不承诺：

- 完全自动 OCR 精准识别。
- 自动判分。
- 人脸识别自动匹配学生。
- 复杂学情长期分析。
- 自动批量营销。

### 1.7 权限、安全与合规技术

认证：

- JWT access token + refresh token。
- 管理端和老师端建议支持密码登录，家长端可支持手机号验证码登录。
- Token 中只放用户 id、角色和会话 id，不放敏感业务数据。

授权模型：

- RBAC：角色包括超级管理员/总校长、校区管理员、老师、家长、学生。
- ABAC：结合 `campus_id`、`class_id`、`student_id` 和家长绑定关系做数据过滤。
- 所有 API 在服务端强制校验权限，前端隐藏按钮不能作为权限边界。

敏感数据：

- 身份证号加密存储。
- 默认脱敏显示：`3201**********1234`。
- 完整查看需要权限，并记录审计日志。
- 导出身份证号需要单独权限。

安全基础：

- HTTPS。
- 上传文件类型和大小限制。
- 图片去除无关 EXIF 信息。
- API rate limit。
- 后端参数校验。
- 操作审计。
- 数据库定期备份。

### 1.8 通知技术栈

MVP 通知先做站内通知和可扩展的推送接口。

通知类型：

- 到托通知。
- 作业反馈通知。
- 请假确认结果。
- 服务到期低频提醒。
- 老师端待确认提醒。
- 管理端风险提醒。

推荐设计：

- `notification` 表保存通知状态。
- `NotificationProvider` 抽象短信、公众号、小程序订阅消息、App Push。
- MVP 可先实现站内通知 + 预留外部通道。
- 通知发送使用队列，避免阻塞主流程。

### 1.9 测试与质量工具

代码质量：

- ESLint。
- Prettier。
- TypeScript strict mode。
- Husky + lint-staged。
- Commit message 规范可选。

测试工具：

- Vitest：前端工具函数、组件逻辑测试。
- React Testing Library：前端组件行为测试。
- Jest：后端单元测试。
- Supertest：后端 API 集成测试。
- Playwright：端到端流程和关键页面截图测试。
- Prisma migration test：数据库迁移验证。
- k6 或 Artillery：基础性能压测。

质量门禁：

- PR 必须通过类型检查。
- PR 必须通过单元测试。
- 核心业务 API 必须有集成测试。
- 上线前必须跑 E2E 主流程。
- 权限相关变更必须补权限测试。

---

## 2. 开发计划

### 2.1 阶段 0：项目初始化

目标：搭建可运行的工程骨架。

开发内容：

- 创建 Monorepo。
- 初始化 `apps/web`、`apps/api`、`packages/db`、`packages/shared`、`packages/ui`。
- 配置 TypeScript、ESLint、Prettier。
- 配置 Docker Compose：PostgreSQL、Redis、MinIO。
- 配置 Prisma schema 初版。
- 配置环境变量模板。
- 配置基础 CI：类型检查、lint、测试。

验收标准：

- 本地一条命令启动前端、后端、数据库、Redis、MinIO。
- API health check 正常。
- 前端能访问登录页。
- Prisma migration 能成功执行。

### 2.2 阶段 1：认证、角色和多校区权限

目标：先打牢权限边界，避免后续业务接口返错数据。

开发内容：

- 用户登录、退出、refresh token。
- 角色模型：超级管理员/总校长、校区管理员、老师、家长、学生。
- 校区管理：新增、编辑、启用、停用、负责人。
- 用户与校区授权关系。
- 老师与校区、班级授权关系。
- 家长与学生绑定关系。
- 权限中间件和数据范围过滤工具。
- 操作审计基础表。

关键接口：

- `POST /auth/login`
- `POST /auth/refresh`
- `GET /me`
- `GET /campuses`
- `POST /campuses`
- `PATCH /campuses/:id`
- `POST /users/:id/campuses`

验收标准：

- 总校长可查看全部校区。
- 校区管理员只能查看授权校区。
- 老师只能进入授权校区和负责班级。
- 家长只能看到绑定孩子。
- 未授权访问返回 403。

### 2.3 阶段 2：学生档案、班级和托管类型

目标：完成托管日常运营的基础资料。

开发内容：

- 固定 4 种托管类型：中午托、下午托、晚辅导、晚全托。
- 学生档案：姓名、性别、身份证号、学校、年级、校区、班级、托管类型、状态、备注。
- 身份证号加密存储和脱敏展示。
- 家长绑定。
- 班级管理：校区、年级、容量、负责老师、学生分班。
- 学生状态：在读、停读、请假中。
- 批量导入预留。

关键接口：

- `GET /students`
- `POST /students`
- `PATCH /students/:id`
- `GET /classes`
- `POST /classes`
- `POST /classes/:id/students`
- `GET /service-types`

验收标准：

- 创建学生时必须选择校区和托管类型。
- 老师只能看到负责班级学生。
- 家长端孩子信息默认脱敏身份证号。
- 管理员完整查看身份证号时写入审计日志。

### 2.4 阶段 3：学生考勤、拍照签到和家长通知

目标：跑通“到托安全确认”闭环。

开发内容：

- 老师工作台按校区、班级、托管类型筛选学生。
- 学生状态：已到、请假、缺勤、迟到、已离托、待确认。
- 拍照签到上传到托照片。
- 老师选择学生后拍照；拍照后人工确认匹配。
- 匹配失败进入待确认，不自动通知家长。
- 家长端到托通知和到托照片查看。
- 通知发送记录。

关键接口：

- `POST /attendance/check-in`
- `POST /attendance/:id/confirm`
- `GET /attendance/today`
- `GET /parent/students/:studentId/attendance`
- `GET /files/signed-url`

验收标准：

- 老师可拍照签到。
- 签到成功后家长收到到托通知。
- 家长只能看到自己孩子的到托照片。
- 匹配失败不会通知家长。
- 签到记录关联校区、班级、老师和学生。

### 2.5 阶段 4：老师考勤

目标：为老师课费和班级核算提供依据。

开发内容：

- 老师到岗签到、离岗签退。
- 签到关联校区、班级、服务时段。
- 管理员补签和审核。
- 异常状态：迟到、早退、缺勤、请假、补签待审。
- 管理端老师考勤异常列表。

关键接口：

- `POST /teacher-attendance/check-in`
- `POST /teacher-attendance/check-out`
- `POST /teacher-attendance/:id/adjust`
- `GET /teacher-attendance`

验收标准：

- 老师能签到和签退。
- 管理端能按校区查看老师考勤异常。
- 老师考勤能进入班级核算。

### 2.6 阶段 5：作业拍照批改、家长反馈和错题本

目标：跑通“老师批改 -> 家长真实查看 -> 错题沉淀”闭环。

开发内容：

- 老师进入校区、班级和学生。
- 上传作业原图。
- AI 提供圈错建议。
- 老师确认、修改或忽略圈错区域。
- 生成批改图片。
- 填写或生成三类点评：行为表现、作业完成、知识掌握。
- 老师确认发布给家长。
- 家长端查看作业原图、批改图片、三类点评。
- 老师确认的错题自动收录错题本。
- 错题记录学科、知识点、错因、图片区域、订正状态。

关键接口：

- `POST /homework-reviews`
- `POST /homework-reviews/:id/images`
- `POST /homework-reviews/:id/ai-suggest-areas`
- `PATCH /homework-reviews/:id/confirm-areas`
- `PATCH /homework-reviews/:id/feedback`
- `POST /homework-reviews/:id/publish`
- `GET /parent/students/:studentId/homework-reviews`
- `GET /mistake-book`

验收标准：

- 老师发布前家长不可见。
- 家长能看到作业原图。
- 家长能看到老师确认后的批改图片。
- 家长能看到行为表现、作业完成、知识掌握三类点评。
- 确认过的错题进入错题本。

### 2.7 阶段 6：AI 同类题和 Word 练习单

目标：完成错题本第一版价值闭环。

开发内容：

- 基于错题生成同类题草稿。
- 老师选择需要加入练习单的题目。
- 生成 Word 练习单。
- 练习单包含学生、班级、日期、学科、原错题摘要、同类题、答题区、老师备注。
- 文件保存到对象存储。
- 生成记录进入 AI 日志。

关键接口：

- `POST /mistake-book/:id/generate-similar-questions`
- `POST /worksheets`
- `GET /worksheets/:id/download-url`

验收标准：

- AI 生成题目默认草稿。
- 老师确认后才生成 Word。
- Word 文件可下载。
- 不承诺自动判分和完整题库。

### 2.8 阶段 7：收费、服务有效期和班级核算

目标：跑通机构经营最小核算。

开发内容：

- 月缴和学期缴。
- 学生服务周期、有效期、应收、实收、余额/欠费、到期时间。
- 家长端只展示服务有效期和续费提示。
- 老师端按权限查看服务到期和欠费提醒。
- 校长/管理员端查看完整收费、余额/欠费。
- 低频提醒：到期前一次、到期当天一次；逾期后后台手动提醒。
- 校区/班级核算：应到、实到、请假、缺勤、学生出勤收入、老师课费、预估毛利。

关键接口：

- `POST /billing-records`
- `GET /billing-records`
- `GET /parent/students/:studentId/service-period`
- `GET /settlements/class`
- `POST /settlements/generate`

验收标准：

- 管理端能看余额/欠费。
- 老师端能看服务到期和欠费提醒。
- 家长端不能看到余额和欠费金额。
- 班级核算支持校区筛选。
- AI 不能修改收费金额、余额/欠费和课费。

### 2.9 阶段 8：AI 助手和 AI 操作日志

目标：实现可控、可追溯的 AI 辅助入口。

开发内容：

- 家长 AI：查到托、查作业、请假、留言、查服务有效期。
- 老师 AI：快捷录入考勤、作业反馈、点评草稿。
- 管理 AI：查询校区经营数据、班级核算、风险提醒。
- 风险分级。
- 写入确认卡片。
- 高风险拒绝或引导传统页面。
- AI 操作日志。

关键接口：

- `POST /ai/intent`
- `POST /ai/actions/:id/confirm`
- `GET /ai-action-logs`

验收标准：

- 家长问“今天作业完成了吗”，返回作业状态、原图、批改图和三类点评摘要。
- 家长问“什么时候到期”，只返回服务有效期和续费说明。
- 老师用 AI 生成反馈草稿后必须确认。
- 跨校区转移学生被判定为高风险，不执行。
- 所有 AI 动作有日志。

### 2.10 阶段 9：前端完整页面和设计落地

目标：将 Serenity-Neumorphic UI 设计稿转为可用页面。

页面清单：

- 登录/角色入口。
- 管理端：首页看板、校区管理、学生与班级、考勤/作业/收费、AI 日志。
- 老师端：今日托管、AI 快捷录入、作业批改与点评、班级日报。
- 家长端：首页、AI 助手、作业/考勤详情、我的/服务信息。
- 学生端：今日任务、错题本/学习入口。

验收标准：

- 所有页面都接真实 API 或 mock API。
- 角色切换后权限正确。
- 移动端无横向滚动。
- 家长端图片查看、点评、服务有效期展示完整。
- 老师端高频操作不超过 3 步完成。

### 2.11 阶段 10：上线准备

目标：具备试点机构部署条件。

开发内容：

- 生产环境 Docker 配置。
- Ubuntu Server 部署手册。
- `docker-compose.prod.yml` 生产编排文件。
- Nginx 反向代理和 HTTPS 配置。
- `/opt/afterclass` 生产目录规划。
- 数据库迁移脚本。
- Seed 数据和初始化超级管理员。
- 文件存储配置。
- 日志采集。
- 错误监控。
- PostgreSQL 定时备份和恢复脚本。
- MinIO 文件数据备份策略。
- 防火墙和端口开放策略。
- 操作手册。

验收标准：

- 新环境可从空库初始化。
- Ubuntu 22.04 LTS 或 24.04 LTS 测试机可完成全新部署。
- `web`、`api`、`postgres`、`redis`、`minio`、`nginx` 容器全部正常启动。
- 域名 HTTPS 可访问。
- 核心流程可跑通。
- 数据库可备份和恢复。
- 服务器重启后服务可自动恢复。
- 防火墙只开放必要端口。
- 日志、图片、数据库数据不会因容器重建丢失。
- 管理员有初始化账号和权限。

---

## 3. 测试计划

### 3.1 测试分层

测试分为 6 层：

1. 单元测试：函数、权限判断、金额计算、脱敏规则。
2. API 集成测试：接口、数据库、事务、权限过滤。
3. 前端组件测试：表单、弹窗、确认卡片、图片查看。
4. E2E 测试：跨角色完整业务流程。
5. 安全与权限测试：越权、敏感信息、文件访问。
6. 性能与稳定性测试：列表、图片上传、AI 队列、并发查询。

### 3.2 单元测试范围

后端单元测试：

- 托管类型规则。
- 校区权限过滤。
- 家长绑定关系判断。
- 身份证号脱敏。
- 身份证号加密/解密封装。
- 家长端费用字段过滤。
- 老师课费计算。
- 班级毛利计算。
- 到期提醒日期判断。
- AI 风险分级。
- AI 确认动作状态机。

前端单元测试：

- 表单校验。
- 角色路由守卫。
- 家长作业详情组件。
- 图片预览组件。
- 三类点评组件。
- 费用展示组件：家长端不显示余额。
- 校区筛选组件。

### 3.3 API 集成测试范围

认证与权限：

- 未登录访问返回 401。
- 非授权角色访问返回 403。
- 校区管理员不能访问其他校区数据。
- 老师不能访问非负责班级学生。
- 家长不能访问非绑定孩子。

学生与班级：

- 创建学生必须选择校区和托管类型。
- 学生身份证号保存后默认脱敏返回。
- 完整身份证查看需要权限并生成审计日志。
- 班级必须关联校区。

考勤：

- 老师拍照签到生成考勤记录。
- 匹配失败状态为待确认，不发送家长通知。
- 确认签到后发送通知。
- 家长只能获取自己孩子到托照片。

作业：

- 老师上传作业原图。
- AI 圈错建议生成草稿。
- 老师确认后生成批改图。
- 发布后家长可见。
- 未发布作业家长不可见。
- 错题自动进入错题本。

收费：

- 创建月缴记录。
- 创建学期缴记录。
- 管理端返回余额/欠费。
- 老师端按权限返回服务到期和欠费提醒。
- 家长端只返回服务有效期和续费提示。

AI：

- 低风险查询直接返回。
- 中风险写入生成确认卡片。
- 高风险返回拒绝和传统页面引导。
- AI 日志记录完整。

### 3.4 E2E 主流程

流程 1：多校区建档

1. 总校长登录。
2. 创建阳光校区。
3. 创建校区管理员。
4. 创建老师并授权阳光校区。
5. 创建班级并分配老师。
6. 创建学生并绑定家长。
7. 校区管理员只能看到阳光校区数据。

通过标准：

- 数据关联校区。
- 权限隔离正确。
- 总校长可查看汇总。

流程 2：拍照签到和家长通知

1. 老师登录。
2. 进入阳光校区三年级班级。
3. 选择王小雨拍照签到。
4. 系统生成到托记录。
5. 家长端收到到托通知。
6. 家长查看时间、托管类型和到托照片。

通过标准：

- 签到记录正确。
- 家长只看到自己孩子。
- 图片 URL 非公开。

流程 3：作业批改和家长反馈

1. 老师进入班级和学生。
2. 上传作业原图。
3. AI 生成圈错建议。
4. 老师确认圈错区域。
5. 老师填写或确认三类点评。
6. 老师发布反馈。
7. 家长查看作业原图、批改图片、行为表现、作业完成、知识掌握。
8. 错题进入错题本。

通过标准：

- 未发布前家长不可见。
- 发布后图片和点评完整。
- 错题本记录正确。

流程 4：错题本和 Word 练习单

1. 老师打开学生错题本。
2. 选择一条错题。
3. AI 生成 3 道同类题。
4. 老师选择其中 2 道。
5. 系统生成 Word 练习单。
6. 老师下载打印。

通过标准：

- 同类题为草稿。
- Word 内容完整。
- 文件权限正确。

流程 5：服务有效期和费用权限

1. 管理员创建月缴收费记录。
2. 设置服务有效期。
3. 管理端查看应收、实收、余额/欠费。
4. 老师端查看学生到期提醒。
5. 家长端查看服务有效期。

通过标准：

- 家长端无余额、欠费金额。
- 老师端无班级毛利。
- 管理端有完整财务视图。

流程 6：AI 请假确认

1. 家长输入“明天下午请半天假”。
2. AI 识别学生、时间、类型和原因。
3. 系统生成请假确认卡片。
4. 家长确认。
5. 老师端出现请假待确认。
6. AI 日志记录全过程。

通过标准：

- 未确认不写入。
- 确认后进入业务流程。
- 日志可追溯。

流程 7：AI 高风险拒绝

1. 校区管理员输入“把王小雨转到星河校区”。
2. AI 判定跨校区转移为高风险。
3. 系统不执行。
4. 引导进入传统学生管理页面。
5. 记录 AI 日志。

通过标准：

- 高风险不写入。
- 无数据变更。
- 有日志。

### 3.5 权限和安全测试

重点用例：

- 家长 A 请求家长 B 孩子的作业图片，返回 403。
- 老师访问非负责班级学生，返回 403。
- 校区管理员访问其他校区收费记录，返回 403。
- 家长端调用管理端收费接口，返回 403。
- 家长端作业详情响应不包含余额、欠费、毛利、老师课费字段。
- 身份证号默认脱敏。
- 完整身份证查看记录审计日志。
- 对象存储原始 URL 不可公开访问。
- 签名 URL 过期后不可访问。
- 上传非图片文件被拒绝。
- 超大图片被拒绝。

### 3.6 AI 测试

AI 不按“输出看起来合理”验收，必须按结构化结果验收。

测试范围：

- 意图识别准确率。
- 实体识别完整性。
- 风险等级正确性。
- 写入确认卡片字段完整性。
- 高风险拒绝。
- 失败降级。
- AI 日志完整性。

固定测试样例：

```text
孩子到托了吗？
今天作业完成了吗？
明天下午请半天假。
给老师说孩子今天咳嗽。
什么时候到期？
今天哪个校区毛利最低？
把王小雨转到星河校区。
删除停读学生陈一诺。
把欠费金额改成 0。
```

通过标准：

- 家长查询作业返回原图、批改图和三类点评摘要。
- 家长查询到期不返回余额或欠费金额。
- 经营查询遵守校区权限。
- 高风险操作不执行。
- AI 失败时提示人工确认或传统页面。

### 3.7 性能测试

MVP 性能目标：

- 管理端首页接口 P95 小于 800ms。
- 老师今日托管列表 P95 小于 600ms。
- 家长首页 P95 小于 600ms。
- 图片上传单张 5MB 内正常完成。
- 100 并发查询家长首页无错误。
- 50 并发老师签到无重复记录。
- AI 异步任务排队可查询状态。

压测场景：

- 1000 名学生、3 个校区、40 个班级。
- 单校区 200 名学生同时签到查询。
- 50 名老师同时上传作业图片。
- 200 名家长同时查看作业反馈图片。
- 管理端按校区生成核算报表。

### 3.8 兼容性测试

浏览器：

- Chrome 最新稳定版。
- Edge 最新稳定版。
- Safari iOS。
- 微信内置浏览器。

设备：

- 管理端：1366x768、1440x900、1920x1080。
- 老师端：iPad 横屏、iPad 竖屏、常见安卓平板。
- 家长端：iPhone、安卓主流机型。
- 学生端：手机和 10 英寸平板。

重点：

- 移动端无横向滚动。
- 底部导航不遮挡内容。
- 图片查看和缩放可用。
- 表格在移动端不强行塞入小屏。

### 3.9 UAT 验收测试

参与角色：

- 校长/管理员。
- 校区管理员。
- 老师。
- 家长代表。
- 学生代表。

验收内容：

- 校区和权限是否符合真实管理方式。
- 老师拍照签到是否高效。
- 老师拍照批改是否可操作。
- 家长看到作业原图和批改图是否清楚。
- 三类点评是否满足家校沟通。
- 收费到期提醒是否不过度打扰。
- 班级核算是否能支撑校长决策。
- AI 是否可控，不会乱写入。

---

## 4. 交付节奏

建议 10 周完成 MVP 试点版。

### 第 1 周：工程初始化和架构基础

- Monorepo。
- Docker 本地环境。
- 数据库 schema 初版。
- CI 基础。
- 登录页面和健康检查。

### 第 2 周：认证、多校区和权限

- 登录、角色。
- 校区管理。
- 权限中间件。
- 数据范围过滤。
- 审计日志基础。

### 第 3 周：学生、班级、托管类型

- 学生档案。
- 身份证号加密脱敏。
- 家长绑定。
- 班级分配。
- 老师授权。

### 第 4 周：学生考勤和拍照签到

- 老师今日托管。
- 拍照签到。
- 到托照片。
- 家长通知。
- 家长首页。

### 第 5 周：老师考勤和作业上传

- 老师签到签退。
- 老师考勤异常。
- 作业原图上传。
- 作业批改草稿。

### 第 6 周：AI 圈错、三类点评和家长作业详情

- AI 圈错建议。
- 老师确认圈错。
- 批改图片。
- 三类点评。
- 家长端作业详情。

### 第 7 周：错题本和 Word 练习单

- 错题自动收录。
- AI 同类题。
- 老师选择题目。
- Word 练习单生成。

### 第 8 周：收费、服务有效期和班级核算

- 月缴、学期缴。
- 余额/欠费。
- 家长服务有效期。
- 老师到期提醒。
- 校区/班级核算。

### 第 9 周：AI 助手和操作日志

- 家长 AI 助手。
- 老师 AI 快捷录入。
- 管理 AI 经营查询。
- 风险分级。
- AI 操作日志。

### 第 10 周：联调、测试、修复和试点上线

- E2E 主流程。
- 权限安全测试。
- 性能测试。
- UAT。
- 部署文档。
- 试点数据初始化。

---

## 5. 测试数据准备

基础数据：

- 3 个校区：阳光校区、星河校区、城南校区。
- 6 个年级。
- 12 个班级。
- 30 名老师。
- 300 名学生。
- 300 名家长账号。
- 4 种托管类型。
- 月缴和学期缴收费记录。

业务样例：

- 正常到托学生。
- 请假学生。
- 缺勤学生。
- 服务即将到期学生。
- 欠费学生。
- 有作业原图但未批改学生。
- 有批改图但未发布学生。
- 已发布三类点评学生。
- 有错题本学生。
- 需要生成 Word 练习单学生。

AI 样例：

- 家长请假。
- 家长查询作业。
- 家长查询服务有效期。
- 老师口述作业反馈。
- 老师请求圈错。
- 校长查询校区毛利。
- 高风险跨校区转移。

---

## 6. 上线验收清单

上线前必须完成：

- 数据库迁移通过。
- Seed 初始化通过。
- 所有核心 API 有集成测试。
- 所有 E2E 主流程通过。
- 权限越权测试通过。
- 家长端确认不展示余额/欠费金额。
- 家长端作业详情显示原图、批改图和三类点评。
- 老师端能完成拍照签到和作业批改。
- 管理端能按校区查看看板和核算。
- AI 高风险操作不会执行。
- 图片文件非公开访问。
- 身份证号默认脱敏。
- 审计日志记录敏感操作。
- 备份和恢复流程可执行。
- Ubuntu 生产部署验收通过：Docker Compose 服务全部健康、HTTPS 可访问、服务器重启后自动恢复。
- 防火墙只开放必要端口，数据库、Redis 和 MinIO 管理端不暴露公网。
- PostgreSQL 备份文件可生成，并能在测试环境恢复。
- 图片文件和 Word 文件持久化验证通过，容器重建后数据不丢失。

---

## 7. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| AI 圈错不稳定 | 老师不信任结果 | 第一版定位为建议，必须老师确认，可手动圈选 |
| 图片上传量大 | 存储和加载慢 | 对象存储、压缩、缩略图、懒加载、签名 URL |
| 多校区权限复杂 | 数据越权风险 | 服务端统一数据范围过滤，权限测试优先 |
| 家长误解费用信息 | 沟通成本增加 | 家长端只显示服务有效期和续费提示，不显示余额 |
| 身份证号敏感 | 合规风险 | 加密、脱敏、审计、最小权限 |
| 老师操作负担重 | 推广困难 | 老师端批量操作、AI 草稿、少步骤确认 |
| 班级核算公式变化 | 返工 | MVP 将课费公式配置化，先保留字段和计算入口 |
| 通知渠道未确定 | 上线受阻 | 先做站内通知和 provider 抽象，外部通道可替换 |

---

## 8. 开发完成定义

一个功能只有同时满足以下条件，才视为完成：

- 页面可操作。
- API 已实现。
- 数据已落库。
- 权限已校验。
- 错误状态有反馈。
- 关键路径有测试。
- 操作日志或审计日志按要求记录。
- 符合家长端、老师端、管理端的数据可见规则。
- 通过对应验收场景。

---

## 9. MVP 交付物

必须交付：

- 可运行前端。
- 可运行后端 API。
- 数据库迁移脚本。
- 初始化数据脚本。
- Docker 本地/测试环境部署脚本。
- Ubuntu 生产部署手册 `README-DEPLOY-UBUNTU.md`。
- 生产编排文件 `docker-compose.prod.yml`。
- 生产环境变量模板 `.env.production.example`。
- 数据库备份脚本 `scripts/backup-db.sh`。
- 数据库恢复脚本 `scripts/restore-db.sh`。
- 服务健康检查脚本 `scripts/healthcheck.sh`。
- API 文档。
- 管理端、老师端、家长端、学生端页面。
- 核心测试用例。
- 测试报告。
- 部署说明。
- 操作手册。

可选交付：

- DOCX/PDF 测试报告。
- 演示数据包。
- 演示脚本。
- UI 组件规范。
